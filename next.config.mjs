/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: {
    unoptimized: true,
    },
    basePath: "/ViMocViet",
    assetPrefix: "/ViMocViet",
};

export default nextConfig;
